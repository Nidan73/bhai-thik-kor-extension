{
  "name": "bhai-thik-kor-extension",
  "version": "1.1.3",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run clean && vite build --watch & vite build --config vite.content.config.ts --watch",
    "dev:content": "vite build --config vite.content.config.ts --watch",
    "build": "tsc --noEmit && npm run clean && vite build && vite build --config vite.content.config.ts && node scripts/check-content-script.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rimraf dist",
    "package": "npm run build && rimraf bhai-thik-kor-1.1.3.zip && cd dist && bsdtar -a -cf ../bhai-thik-kor-1.1.3.zip *"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "autoprefixer": "^10.4.21",
    "postcss": "^8.5.3",
    "rimraf": "^6.0.1",
    "tailwindcss": "^3.4.17",
    "typescript": "^5.7.3",
    "vite": "^6.3.5",
    "vitest": "^4.1.10"
  }
}
